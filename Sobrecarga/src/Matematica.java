
public class Matematica {
	int somar (int x, int y){ //Método que soma 2 numeros e os retorna no console, com o nome somar.
		 
		 return x + y;
 }
	
	
	int somar (int x, int y, int z){ //Método que soma 3 numeros  e os retorna no console com o nome somar, mostrando assim a sobrecarga de métodos(Métodos do mesmo nome porém com "requisições diferentes").
		
	return	x + y + z; 
	}
	
	
	double somar (double x , double y) { // Método com o nome somar, porém soma dois double  e os retorna no console.
		
		return x + y;
	}
	
}
